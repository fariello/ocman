# IPD produced by this run

- IPD: `.agents/plans/pending/20260725-1043-01-assess-documentation.md`
- Summary: Corrects README to match shipped 1.3.0 behavior. 7 proposed changes (all Low
  Remediation Risk): fix the `kill` synopsis to `[PID|PATTERN]` + enriched preview (High);
  remove the false `OCMAN_CONFIG_PATH` env-override claim (Medium); move `spend` to top-level
  verbs (Medium); document `lr --long`; point `session export` at project export; add an
  `[Unreleased]` CHANGELOG entry; add a consolidated Platforms note.
- Status at write: to-review.
