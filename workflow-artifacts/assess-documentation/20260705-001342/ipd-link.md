# IPD link

- IPD: `.agents/plans/pending/20260705-assess-documentation.md`
- Summary: Align README/ARCHITECTURE with actual behavior — fix the README config template's dead `default_model` key (→ `default_compaction_model`, default `""`), complete the Argument Reference table (~13 missing flags), document the `preprocess_argv` natural-language commands, add `css/` to the TUI layout, and add a CHANGELOG documentation note. Doc-only, Low Remediation Risk; the `Orsession` code rename is deferred.
