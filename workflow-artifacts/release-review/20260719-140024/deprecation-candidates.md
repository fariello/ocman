# Deprecation candidates

- None found this run. The removed `--show-models` / `--list-projects` user flags leave only
  historical docstrings/comments (intentional, not misleading to users). No dead code,
  unused files, or superseded artifacts identified in Section 1.
