# IPD: TUI parity Phase 5 - breadth (project bundles, move, backup clean, search, filter)

- Date: 2026-07-18
- Concern: functionality (CLI<->TUI parity), ui-ux
- Scope: project export/import, session + project move, backup clean, content search, and
  (pending OQ) filter, in `ocman_tui/`. Closes the remaining parity gaps.
- Status: to-review
- Author: its_direct/pt3-claude-opus-4.8
- Set: tui-parity
- Order: 5

## Workflow history

- 2026-07-18 created from the tui-parity umbrella (its_direct/pt3-claude-opus-4.8): the
  breadth phase, split out for focused review. This is the largest by feature count; it may
  be split further at plan-review if any item proves complex.

## Goal

Bring the remaining CLI capabilities into the TUI so parity is complete: project `.ocbox`
export/import, session move (and local project move), pruning old backup archives, content
search, and (pending OQ-4) the `filter` re-scope tool.

## Project conventions discovered (Step 0)

- Conventions as in the umbrella (no dashes, path-scoped commits, worker threads, tabs).
- Reuse points: `bundle_project_data` / `extract_and_import_project` (project .ocbox);
  `db_move_session_metadata`, `db_move_project_metadata`, `move_directory_structure`,
  `db_rebase_paths` (already imported into `core.py` but unused); the backup-clean logic
  behind `backup clean` (`cli_clean_backups`); `db_search_sessions(...)` for content search;
  the top-level `filter` logic if OQ-4 says include it.
- Current TUI: `ExportSessionModal`/`ImportSessionModal` are session-scoped; export button
  disabled for project selections; `MoveProjectModal` does a local physical move only.

## Findings (from the umbrella)

| ID | Severity | Remediation Risk | Persona | Area | Finding | Evidence |
|----|----------|------------------|---------|------|---------|----------|
| T-09 | Medium | Medium | PU | bundles | project export & import absent | CLI `bundle_project_data`/`extract_and_import_project` |
| T-10 | Low | Low | PU | move | session move absent; project move local-only; move/rebase imports unused | `core.py:50-51` |
| T-11 | Low | Low | PU | backup | backup clean absent | CLI `backup clean` |
| T-12 | Low | Med | PU | search | content search absent | CLI `db_search_sessions` |
| T-13 | Low | Med | PU | filter | filter (LLM re-scope) absent | CLI `filter` |
| T-15 | Low | Low | PU | rebase | db rebase absent (import unused) | `core.py:51` |

## Proposed changes (ordered, validatable)

| Step | Source | Change | Files | Rem.Risk | Validation |
|------|--------|--------|-------|----------|------------|
| 1 | T-09 | Enable export for project selections (call `bundle_project_data`); extend the import modal to auto-detect bundle kind (session vs project) and route to `extract_and_import_session`/`extract_and_import_project`. | `ocman_tui/app.py` | Medium | TUI test: export a project bundle; import it back (round-trip). |
| 2 | T-10/T-15 | Add a session move action and a local project move (metadata + optional physical), reusing `db_move_session_metadata`/`db_move_project_metadata`/`move_directory_structure`; wire `db_rebase_paths` if a bulk-rebase action is included. Remote/git-aware runbook: see OQ-3 (may stay CLI-only). | `ocman_tui/app.py` | Low/Med | TUI test: local session/project move updates DB paths; parity vs CLI metadata-only move. |
| 3 | T-11 | Add a "prune old backups" action (age input) to the Database Admin / backup card, calling the backup-clean logic. | `ocman_tui/widgets/database.py`, `core.py` | Low | TUI test: old archives removed; recent kept. |
| 4 | T-12 | Add a content search box (in a tab or the sidebar) that runs `db_search_sessions` in a worker and lists hits, selecting a hit into the sidebar. | `ocman_tui/app.py`/widget | Med | TUI test: a seeded match is found and selectable. |
| 5 | T-13 | (Pending OQ-4) If filter is in scope, add a filter action to re-scope a recovery/compacted doc via the LLM, mirroring the CLI `filter` guards (size cap, secret scan). | `ocman_tui/app.py` | Med | TUI test with mocked LLM: produces the scoped output; guards honored. |

## Deferred / out of scope (with reason)

| Finding ID | Remediation Risk | Axis | Reason | Recommended later step |
|------------|------------------|------|--------|------------------------|
| T-10 (remote/git-aware move) | Med | Complexity | The remote `host:/path` runbook + `--confirm-remote-delete` may not belong in a TUI; confirm at OQ-3. | Keep CLI-only unless OQ-3 says otherwise. |

## Scope check

- Over-scope: guard against building the full remote-move runbook in the TUI if OQ-3 says
  local-only is sufficient for parity. Filter (OQ-4) may be excluded if not wanted.
- Under-scope: none if OQ-3/OQ-4 are answered "include".

## Required tests / validation

- `PYTHONPATH=. /home/gfariello/venv/p3.14/bin/pytest -q` (paste real output).
- TUI tests for each wired item + parity spot-checks vs the CLI equivalents.

## Spec / documentation sync

Update the README/ARCHITECTURE TUI section (consolidated pass) and CHANGELOG for Phase 5.
After Phase 5, the umbrella roadmap moves to `executed/`.

## Open questions

- OQ-3 (from umbrella): does the TUI need remote/git-aware move, or is local metadata move
  enough (remote stays a CLI runbook)? Decides Step 2's scope.
- OQ-4 (from umbrella): is `filter` wanted in the TUI? Decides whether Step 5 runs.
- OQ-P5-1: at plan-review, decide whether this phase should be split into smaller IPDs
  (e.g. bundles; move; backup-clean+search; filter) given its breadth.

## Approval and execution gate

Run `plan-review` (and consider splitting), then on approval execute, validate, and move to
`executed/`.
