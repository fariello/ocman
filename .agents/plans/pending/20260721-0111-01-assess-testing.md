# IPD: testing rigor - close the real coverage gaps + kill the TUI flake class

- Date: 2026-07-21
- Concern: testing (assessed via /assess testing; scope: whole test suite)
- Scope: `tests/*.py`, `pyproject.toml` (dev deps / coverage config), optionally
  `.github/workflows/ci.yml` (non-gating coverage visibility). No product-code behavior change
  except small testability seams explicitly proposed (T-04). Adds tests + test infra only.
- Status: to-review
- Target version: rides the in-flight 1.3.0 line (test/infra only; no user-facing change).
- Approval: awaiting maintainer review/approval
- Author: its_direct/pt3-claude-opus-4.8

## Workflow history

- 2026-07-21 /assess testing (its_direct/pt3-claude-opus-4.8): assessed; proposed 6 changes.

## Goal / verdict

The suite is **strong where it matters** (452 passed / 2 skipped verified; 70% statement
coverage; critical paths, security, and cross-platform portability genuinely tested with
meaningful assertions). Verdict: **adequate, with targeted gaps worth closing**. This IPD
proposes the highest-value improvements, NOT a coverage-number chase:
1. make coverage VISIBLE (no measurement exists today),
2. eliminate the recurring TUI modal-mount flake class suite-wide,
3. add negative/rollback tests for the uncovered ERROR branches of the destructive/transfer
   functions (the risky, data-integrity paths).

## Project conventions discovered (Step 0)

- Universal fallback principles (no GUIDING_PRINCIPLES.md). Plans `.agents/plans/pending/` ->
  `executed/`; IPD `YYYYMMDD-HHMM-NN-<slug>.md`; Status born `to-review`. No em/en dashes in
  authored prose. Path-scoped commits, never push.
- Test stack: pytest + anyio (dev extra); `PYTHONPATH=. pytest`; CI matrix
  ubuntu/macos/windows x py3.10-3.14 runs bare `pytest` (NO coverage). conftest autouse-
  neutralizes the running-OpenCode guard and skips `real_process_detection` (Linux-only) off-Linux.
- 13 test files, 444 tests, ~9.4k lines. coverage 7.15.0 available in the venv.

## Findings (evidence in workflow-artifacts/assess-testing/20260721-010426/)

| ID | Sev | Rem.Risk | Persona | Finding | Evidence |
|----|-----|----------|---------|---------|----------|
| T-01 | Medium | Low | testing | No coverage measurement: no coverage config, CI runs bare `pytest`. Coverage regressions invisible. | ci.yml:33; pyproject (no [tool.coverage]) |
| T-02 | Medium | Low | QA/QC | ~10 TUI tests `assert isinstance(app.screen, XModal)` right after ONE `pilot.pause()` (no poll) - the exact flake class that forced CI reruns. | test_tui.py:164,199,238,339,383,642 |
| T-03 | Medium | Low | testing | Destructive/transfer functions well-covered on happy path but big UNCOVERED error/rollback branches: `_execute_move` 63 missed, `extract_and_import_project` 59, `db_delete_project_recursive` 51, `db_delete_session_recursive` 42, `cli_restore` 29, `db_delete_sessions_batch` 25 (LIVE/data-integrity). | coverage json (evidence.md) |
| T-04 | Low | Low | software eng | Remote-move / git-decision code (`_gather_git_decisions` 53/95 missed) hard to test (git+remote IO); needs a testable seam. | coverage: _gather_git_decisions @9408 |
| T-05 | Low | Low | testing | TUI weakest-covered: widgets/database.py 53%, app.py 65%; worker/error paths thin. | coverage-summary.txt |
| T-06 | Low | Low | QA/QC | Perf/benchmark tests skip-by-default (OCMAN_BENCHMARK=1), never run in CI -> hot-path regressions uncaught. | test_perf.py:28-31 |
| T-07 | - | - | testing | POSITIVE: suite strong on critical paths/security/portability; 452 passed, 70% coverage. | pytest -q; coverage; test-name grep |

## Proposed changes (ordered, validatable)

| Step | Source | Change | Files | Rem.Risk | Validation |
|------|--------|--------|-------|----------|------------|
| 1 | T-02 | Add a shared test helper `await_screen(pilot, app, screen_cls, timeout=5.0)` (poll `isinstance(app.screen, screen_cls)` with `await pilot.pause(0.05)` up to timeout, then assert), mirroring the existing 50x0.1s loops. Replace every bare `assert isinstance(app.screen, XModal)`-after-single-pause in `tests/test_tui.py` with `await await_screen(...)`. Assertion strength preserved (still fails if the modal never mounts); only the race removed. | tests/test_tui.py (+ conftest or a tests/_tui_helpers) | Low | full suite green on Linux; CI matrix green with NO reruns needed across several runs |
| 2 | T-01 | Add `pytest-cov` (or `coverage`) to the `[project.optional-dependencies] dev` list; add a `[tool.coverage.run] source = ["ocman","ocman_tui"]` section; document a `pytest --cov=ocman --cov=ocman_tui` invocation (README/CONTRIBUTING or a make target). Do NOT hard-gate a threshold (avoids brittle CI). | pyproject.toml, README/CONTRIBUTING | Low | `pytest --cov` runs and prints a report locally |
| 3 | T-01 (optional) | Add a NON-GATING coverage step to CI (e.g. one ubuntu/py3.x cell runs `pytest --cov --cov-report=term-missing` and prints it; does not fail on %). Keeps the existing bare-pytest cells as the gate. | .github/workflows/ci.yml | Low | CI shows a coverage report; no new red |
| 4 | T-03 | Add targeted NEGATIVE/ROLLBACK tests for the uncovered error branches of the destructive/transfer functions, prioritized: (a) `extract_and_import_project`/`_session` mid-operation failure -> transaction rollback leaves the DB unchanged + rollback-backup path; (b) `db_delete_project_recursive`/`db_delete_sessions_batch` partial-failure -> rollback / no partial delete; (c) `cli_restore` failure path. Use fault injection (monkeypatch a mid-op sqlite/exec to raise) + assert state intact. | tests/test_export_import.py, test_ocman.py, test_move.py | Low | new tests fail without the existing rollback logic (mutation-check), pass with it; coverage of the named functions' error branches rises |
| 5 | T-04 | Introduce a testable seam for remote-move decisions: extract the PURE branch-selection logic of `_gather_git_decisions` (given fabricated git state -> which runbook/decision) from the git/IO, and unit-test the pure part with fabricated inputs. Product behavior unchanged (pure refactor); covered by the new tests. | ocman/cli.py (seam only), tests/test_move.py | Low | move tests still pass unchanged; new unit tests exercise the decision branches |
| 6 | T-05, T-06 | (Lower priority) Add focused async tests for the highest-risk TUI worker/error paths in `widgets/database.py` (mutating actions + off-thread worker error handling); and make ONE perf check CI-runnable in report-only mode (or a bounded assertion) so gross hot-path regressions surface. | tests/test_tui.py, tests/test_perf.py, ci.yml | Low | new TUI tests pass; perf step runs in CI report-only |

## Deferred / down-scoped (with named axis)

- **Hard coverage-% gate in CI:** DEFERRED. Remediation Risk Medium-High on the
  functionality/usability axis: a brittle threshold causes false CI failures and ratchet
  fights. Propose measurement + visibility (Steps 2-3) first; a floor can be added later once a
  stable baseline is known. Not dropped, just sequenced.
- **Blanket coverage push to a target %:** WONT-DO per the testing lens (chase behavior, not a
  number); replaced by the targeted Steps 4-6.
- Steps 5 and 6 are LOWER priority than 1-4; may be split into a follow-up if the maintainer
  wants a smaller first cut.

## Required tests / validation

- `PYTHONPATH=. /home/gfariello/venv/p3.14/bin/pytest -q` and PASTE ACTUAL output; must stay
  all-green (currently 452 passed, 2 skipped) plus the new tests.
- Step 4 tests MUST be mutation-checked (fail if the rollback/guard logic is removed).
- Step 1 success criterion is behavioral: several CI matrix runs go green WITHOUT needing a
  rerun of a TUI-timing flake.
- Coverage (Step 2/3) is measured and reported; NOT asserted as a gate in this IPD.

## Spec / documentation sync

- README/CONTRIBUTING: document the `pytest --cov` invocation (Step 2). No user-facing product
  behavior changes.

## Open questions

- Coverage VISIBLE only, or eventually GATED at a floor? (Steps 2-3 assume visible-only.)
- Priority/ordering among T-02 (flake), T-03 (rollback tests), T-05/06 (TUI/perf)? Default
  order above is 1->4 first, 5-6 as a possible follow-up.
- Prefer `pytest-cov` or bare `coverage`? (Plan assumes `pytest-cov` for the `--cov` ergonomics.)

## Approval and execution gate

- This IPD is a PROPOSAL and is NOT auto-executed. A human approves (optionally via
  `plan-review`) before any execution.
- Execution checklist (MUST): before coding, create a TodoWrite step-granular checklist tracking
  each executed step, the new tests, the full-suite run with PASTED output, the docs sync, the
  path-scoped commit(s), and the Status-executed + `git mv` to `executed/`.
- Scope fence: tests + test infra + the single pure-seam refactor (T-04) + coverage config/docs.
  No product behavior change beyond the seam; no new runtime (non-dev) dependency.
- Honesty rule (hard MUST): paste ACTUAL `pytest -q` (and `--cov`) output; never claim a pass
  not run. Step 4 tests must be mutation-checked, not vacuous.
- Commits: path-scoped, NEVER push without approval, NEVER tag.
- Lifecycle: on completion set `Status: executed` and `git mv` this IPD to `executed/`.

Next: human review (optionally `/plan-review`) sets `Status: approved`; then execute per the above.
